/* SELECT ITEMS */
const alert = document.querySelectorAll('.alert');
const form = document.querySelectorAll('.shopping-form');
const shopping = document.getElementById('shopping');
const container = document.querySelectorAll('.shopping-container');
const list = document.querySelectorAll('.shopping-list');
const submitBtn = document.querySelectorAll('.submit-btn');
const clearBtn = document.querySelectorAll('.clear-btn');